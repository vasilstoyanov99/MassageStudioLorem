@model IEnumerable<ReviewServiceModel>

@{ ViewBag.Title = "Reviews"; }

@if (Model?.Count() > 0)
{
    <partial name="_ReviewsPartial" model="@Model" />
}
else
{
    <div class="text-center" style="font-weight: bold">
    @NoReviewsFound
    </div>
}